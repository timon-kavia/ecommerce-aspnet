name: ecommerce-aspnet
services:
  ecommerce-frontend:
    root: ecommerce-aspnet/ecommerce-frontend
    installCommand: npm install
    buildCommand: npm run build
    startCommand: npm run dev -- --host --port 3000
    port: 3000
    env:
      - VITE_API_BASE_URL=http://localhost:3001/api
